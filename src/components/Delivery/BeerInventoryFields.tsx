import { FormInstance } from 'antd';
import React from 'react';

type Props = {
    form: FormInstance<any>;

};

const BeerInventoryFields = (props: Props) => {
    return (
        <div>BeerInventoryFields</div>
    );
};

export default BeerInventoryFields;