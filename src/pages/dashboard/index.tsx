import React from 'react';
import Dashboard from '../../components/Dashboard';

type Props = {};

const Page = (props: Props) => {
    return (
        <Dashboard></Dashboard>
    );
};

export default Page;