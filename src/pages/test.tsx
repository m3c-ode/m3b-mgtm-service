import React from 'react';
import FormTest from '../components/Forms/Delivery/new';

type Props = {};

const NewBeer = (props: Props) => {
    return (
        <FormTest />
    );
};

export default NewBeer;