import React from 'react';
import BeerForm from '../../components/Forms/Beer';

type Props = {};

const NewBeer = (props: Props) => {
    return (
        <BeerForm />
    );
};

export default NewBeer;